# 放进项目后，发送给本地 AI（人工智能代理）

读取 `wow-addon-test/skills/wow-addon-test/SKILL.md`。

按技能检查环境，必要时安装工具本地依赖；读取本项目真实源码，为生命、主资源、职业资源完成薄适配并运行全部已支持的 40 专精基础用例。

不要用示例插件替代当前项目，不要在适配器重写生产算法，不要修改参考基线，也不要把未支持的额外资源算成通过。交付接入文件、实际运行报告、覆盖缺口和定向复跑命令。

通过后，在临时副本中故意引入一个真实风险缺陷，确认测试能抓到，然后恢复。缺失权限、依赖或确实没有参照时，准确报告阻塞点。
