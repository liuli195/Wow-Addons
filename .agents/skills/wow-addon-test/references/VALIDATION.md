# 实际验证记录

本文件描述已经执行的检查，不是计划。

## 宿主与数据

- 宿主：Linux x86-64，Python（编程语言）3.13.5，系统 Lua（脚本运行时）5.4 共享库。
- 基线：40 个唯一专精，846 条人工用例，1,613 个检查点。
- 原生参照：16 个已标注的原生方法摘录；这些方法已在受控输入下执行。没有声称加载完整客户端或所有原生 XML（界面定义）。
- 输入、基线、参照与基础接口环境均保留校验摘要。普通测试命令不改写这些文件。

## 已执行

| 检查 | 结果 |
|---|---|
| 输入结构、唯一专精、唯一用例、基线对应关系 | 通过 |
| 事件型独立示例插件 | 846/846，用例内保持状态，通过 |
| 逐帧采样型独立示例插件 | 846/846，通过 |
| 重复执行 | 输出一致 |
| 原生方法重生成 | 与保存基线完全一致 |
| 布尔与数值区别、缺失字段、数组长度、浮点比较 | 通过 |
| 没有匹配用例、未知专精、重复输出 ID、缺少输出 | 正确拒绝/报错 |
| Lua 语法错误与死循环 | 正确报告/超时终止 |
| 未配置接口、单位事件过滤、计时顺序与取消、空值返回位置 | 通过 |
| 计时器未清理 | 在强制释放环境之前发现并报告 |
| 项目路径越界 | 正确拒绝 |
| 初始化和技能安装覆盖保护 | 通过 |
| 空格及中文项目路径 | 通过 |
| 安装后的技能在不同工作目录调用工具 | 通过 |
| 源码审计脚本的词法处理与标记匹配 | 本地自测通过；网络抓取未执行 |

目前 `reports/selftest.json` 记录 32 项检查全部通过。该结果来自本次宿主，不能替代其他运行时的本机复跑。

## 真正检测到的错误变体

| 故意制造的错误 | 首个具体差异 |
|---|---|
| 连击点容量冻结 | `spec-259.resource-cap-roundtrip` 步骤 2；预期 7，实际 6 |
| 保留旧主资源类型 | 类型往返步骤 2；预期上限 1000，实际 100 |
| 灵魂碎片重复换算 | 预期 2，实际 0.2 |
| 只处理五个符文 | 预期节点数量 6，实际 5 |
| 未注册最大资源变化事件 | 预期上限 200，实际 100 |

这些变体只修改临时复制的示例生产代码，执行后自动删除副本，没有改动原生基线或两个正常示例。

## 未执行，不能宣称通过

| 项目 | 原因/边界 |
|---|---|
| Windows（视窗系统）安装及 Lua 5.1 实跑 | 本次宿主不是 Windows；附有安装入口，需在使用者机器运行自测 |
| 真实 MYUI 接入 | 本次没有用户实际项目源码；需本地代理读取并编写薄适配 |
| 代理自主接入全流程 | 附带技能格式和命令已检查，但没有在 Codex / Claude Code 中启动另一个代理会话实际验收 |
| 完整原生文件字节下载与上游标记复核 | 已通过连接器读取相关原方法；本次未取得全部原文件的可校验字节副本。可执行 `reference/verify_upstream.py` 补充核对 |
| 当前国服真实天赋和配置 | 输入为人工接口场景，不是采集的人物状态 |
| 受限值、污染、受保护执行 | 普通 Lua 无法等同真实客户端机制；未计入通过 |
| 全技能/光环资源 | 明确缺口在逐专精目录和每次报告里 |
| 画面 | 用户已排除；工具不测试像素、布局或视觉素材 |

## “完整第一版”的实际含义

已经有可执行的本地命令、共享技能、固定输入/预期、可运行的适配契约、两种示例、确定性比较、具体差异和覆盖报告。

不是“任何插件免适配”，不是“40 专精的所有职业机制全面验证”。要把当前工具用于真实插件，仍需 AI 按技能完成一次项目专用接入；此后测试无需模型服务。
