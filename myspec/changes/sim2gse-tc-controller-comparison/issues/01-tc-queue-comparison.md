# 01: TC 服务端请求队列双场对照

Status（状态）: implemented-pending-verification
Blocked by（阻塞任务）: None（无）

## What to build

从现有序列评估入口显式选择 TC 服务端施法请求队列策略；保持默认策略及 GSE 步进、SimC 结算不变。用同一最终序列对旧、新两场按键时序分别运行基线与 TC 策略，输出可复核轨迹和事后评分。

## Acceptance criteria

- [x] TC 准入、单槽替换、取消与可执行时取出按固定上游源码实现；窗口外拒绝不改动旧请求，资源和技能自身冷却不在准入时预判。限本次序列不含引导法术的路径。
- [ ] 最高层序列评估入口红灯转绿；默认策略、物品顺序、自动攻击和禁用控制模式回归通过。
- [x] 两场各自只传入探针按键时刻，同一最终序列、角色、种子 `20260914`，不传实测状态；模拟输出冻结后才评分。
- [x] 报告两场技能集合差异、成功频率差异率、时序一致／不一致率、相对固定基线变化及首个分叉；负收益也如实报告。
- [ ] 保存源码身份、输入、轨迹、构建和验证证据；若实验无法覆盖 TC 的客户端或网络行为，明确限定结论。

## Comments

- 2026-09-23：旧控制版本已锁定在 `df5070b1d116b3d878ae66fefd132466df341bdc`，新分支从此提交创建；根目录临时 `native.json` 已原样移入 `.local/` 并校验哈希。
- 2026-09-23：TC 对照以 TrinityCore 固定提交 `a96d89772a96d275f2d40d2aa27e4a5e0252b04a` 的 `Player::CanRequestSpellCast`、`RequestSpellCast`、`CanExecutePendingSpellCastRequest` 和 `ExecutePendingSpellCastRequest` 为依据；源码：https://github.com/TrinityCore/TrinityCore/blob/a96d89772a96d275f2d40d2aa27e4a5e0252b04a/src/server/game/Entities/Player/Player.cpp 。按键时刻暂当服务端收到请求的时刻；客户端筛选、网络传输、引导施法和 TC 版本差异未覆盖，不宣称等于正式服完整控制链。
- 2026-09-23：同一最终 12 步 GSE 序列、同一角色、种子 `20260914`、180 秒。旧场 2568 次点击、新场 2516 次点击；四组模拟输出分别保存在 `.local/sim2gse/experiments/2026-09-23-tc-complete-{old,new}-{controlled,tc}/`，含 `input.simc`、`native.txt`、`native.json`、`invocation.json`、`experiment.json`。运行时只读取序列、角色与点击时刻，输出后用原探针成功事件评分。

  | 场次与策略 | 实际／模拟成功 | 技能集合差异 | 频率差异率 | 时序一致／不一致率 | 首个位置分叉 |
  | --- | ---: | ---: | ---: | ---: | --- |
  | 旧场原控制 | 161／158 | 0 | 22.98% | 68.97%／31.03% | 第 3 个：模拟天灾打击／实测 `putrefy`（中文名待核） |
  | 旧场 TC | 161／155 | 1（模拟缺亡者大军） | 18.63% | 65.82%／34.18% | 第 2 个：模拟脓疮打击／实测亡者大军 |
  | 新场原控制 | 163／156 | 0 | 17.79% | 63.32%／36.68% | 第 3 个：模拟天灾打击／实测 `putrefy`（中文名待核） |
  | 新场 TC | 163／149 | 1（模拟缺亡者大军） | 13.50% | 61.54%／38.46% | 第 2 个：模拟脓疮打击／实测亡者大军 |

  TC 相对原控制：频率差异率旧场下降 4.35、新场下降 4.29 个百分点；时序不一致率旧场上升 3.15、新场上升 1.78 个百分点。旧场轨迹里 1195ms 的亡者大军请求被 1269ms 的黑暗突变请求替换；后者自身冷却尚有 44152ms，在取出后失败。该结果说明单纯搬入 TC 服务端队列，在这些输入下会丢失原本可执行的请求，不能作为已改善时序的方案。
- 2026-09-23：目标测试新增准入／替换、执行时资源检查、执行时技能冷却检查和拒绝实测反馈；首次全套 114 项测试有 1 项无关进程所有权用例在读取刚创建的空 JSON 时竞争失败，单独重跑通过；待正式干净工作树验证。
