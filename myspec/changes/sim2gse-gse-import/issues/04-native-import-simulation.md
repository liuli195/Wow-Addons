# 04：把可模拟的导入序列送入原生 DPS 模拟

Label（标签）: change:task
Triage（分拣）: ready-for-agent
Status（状态）: done

## What to build

用户选定导入序列和版本后，当前角色可按动作得到严格映射，逐次按键计划驱动原生受控引擎；不可映射动作在模拟前被拒绝。

## Blocked by

03：按上游语义编译全部 GSE 控制块。已完成。

## Acceptance criteria

- [x] 可模拟的邪恶死亡骑士样本从真实导入字符串获得 DPS 报告、逐次输入轨迹与条件摘要。
- [x] 空点击占用输入，动作块、来源路径和原生回报保持一致。
- [x] 未知技能、物品、条件、宏命令或环境依赖不进入伤害模拟且有具体诊断。
- [x] 角色原始输入及导入字符串可追溯；现有优化与单次评估行为不变。

## Completion evidence

全部四项由公开导入任务入口及 Sim2GSE 测试验证。失败导入不会运行受控序列伤害模拟；任务保留 `input.original.simc`、`input.gse`、序列与版本、SHA-256、来源路径及编译场景。旧搜索候选仍走原 codec 适配，并由固定基线 golden 测试保护搜索起点和顺序。

真实 DPS 验收由同一公开 12.1 邪 DK 帖的两条原始串完成，原文未修改，GSE 3.3.31（3331），成员版本均为 1：AOE `SOL_UDK_AOE` SHA-256 `ae596b6966ac87e9776fe34a36dca6561d79616c0b9f0a1c8e17afc98991f7f9`；ST `SOL_UDK_ST` SHA-256 `033e5853eb3b4253b7764b57035fd907294568444cc503f11746accac070cf88`。当前角色动作目录通过独立 SimC 查询严格验证了两条串的动作，其中 316239 对应角色当前可用的 `festering_strike`（原生动作编号 85948），1247378 对应已选天赋动作 `putrefy`；其余映射和证据路径见 `docs/sim2gse/gse-import-corpus.md`。

两条串均展开为 47 个点击，逐点击 `source_path`、全套任务产物、模拟条件及 DPS 数值均已记录在语料文档和忽略目录 `.local/sim2gse/import-review-sol-aoe-20260925/`、`.local/sim2gse/import-review-sol-st-20260925-b/`。场景为无修饰键、已有存活可攻击目标、宠物已召唤；点击间隔与输入间隔同为 300 ms。AOE 受控原生模型 DPS 为 37842.240154123254，ST 为 41627.01167663097。两者共享的 77044.71054312987 是同角色自由选择基准 DPS，单独报告，不作为导入结果或直接比较值。其余此前无法严格映射的真实成员仍按具体原因拒绝；没有猜技能、替换 `item 13` 或改动伤害模型。

本票功能验收已完成。原生模拟尚未替代游戏内导入和战斗日志验收，该人工验收仍由 05 跟踪。
