# 自动搜索、复测与恢复

Status（状态）: closed
Triage（分拣）: ready-for-agent
Assignee（领取者）: unassigned

## Parent

[开发需求](../spec.md)

## What to build

用户角色经过同一任务入口自动搜索统一动作序列、独立复测并取得真实候选文本。预算、缓存、取消与恢复贯穿一次完整运行，不建立另一套离线手工调度流程。

## Blocked by

[同一导出序列的原生模拟](02-sequence-simulation.md)

## Acceptance criteria

- [x] 按已确认的多起点局部搜索、分批加测及轨迹反馈实施；技能与主动使用物品同等处理，序列合法性先检查。
- [x] 原生参考、有效初始序列与候选保持相同角色和场景；锁定候选后使用未参与选优的最终样本，保存有效样本数与差异区间。
- [x] 十分钟计算上限、最迟第七分钟结束搜索、最多两个单线程进程真实生效；复测不足明确标注，不能延长预算凑成功。
- [x] 名义、扰动、慢按、暂停和相位情景有实际结果；无可信改善保留有效初始序列，不承诺提高固定百分比。
- [x] 相同完整身份下的成功批次可复用且不重复计数；变化条件、损坏报告或失败结果不误命中缓存。
- [x] 实际中断后清理所属进程，保留完整结果和已用预算；恢复已锁定任务只继续其复测，不影响配装器或其他模拟。
- [x] 输出满足必要一致性检查的真实字符串，独立复测未完成时不能把结果标为已验证。

## Testing Decisions

最高入口沿用前两票的任务启动、取消、恢复与结果读取，覆盖 T17—T20；用受控故障注入检查写入前后中断和版本变化，最终真实角色冒烟仍调用原生引擎。执行细则与示例参数见[运行契约](../../../../docs/sim2gse/runtime-contract.md)，不要把旧测量的 1.82 倍当成搜索保证。

## Specification coverage

`Sim2GSE reports independently tested improvements`、`Sim2GSE respects the confirmed runtime budget`、`Sim2GSE resumes without mixing results`，以及真实导出要求。

## Completion evidence

实施、30 项针对性回归和默认预算真实角色验收完成；全仓正式验证按用户安排延后，独立审查结论见下。

### 2026-09-12 实施记录

沿用 `run_task`（执行任务）公开接缝，默认执行优化；前两票回归显式使用单次模式。新增同模块的启动、取消、恢复与读取入口。主代理接手实施后完成以下可复跑证据：

- 锁定候选后恢复检查先失败（未暴露最终阶段），补齐持久化检查点后通过；恢复保持候选编号并累计已用时间。
- 超过 600 秒及非有限预算检查先失败，补齐配置边界后通过。
- 跨任务相同语义输入的搜索缓存复用先失败，接入成功批次缓存后通过；最终样本使用任务独立编号。
- 损坏但仍可解析的报告重新计算；配置变化拒绝原地恢复；真实任务硬退出后所属引擎退出，恢复仍使用原锁定候选。
- 补充系统边界检查：两个独立任务各有子孙进程，强杀一方只清理该方全部后代，另一方保持存活。
- 22 项项目自动检查通过，96.361 秒；旧控制器关闭回归曾暴露报告文件名变化，修复后单项通过并包含在上述全量结果内。

已登记真实角色完整搜索检查 `tests/sim2gse/manual_search_task.py`，测试入口清单检查通过。全仓已有构建与验证失败仍按用户安排留到拉取请求合并前处理。

### 2026-09-12 最终针对性验收

- 最终项目回归：`.venv/Scripts/python.exe -m unittest discover -s tests/sim2gse -p test_*.py`，30 项通过，126.497 秒。测试均集中于仓库根 `tests/sim2gse/`；`tests/dev/test_inventory.py` 通过，完整搜索入口已登记到构建与验证配置。
- 审查修复继续采用先失败再通过：原生全局错误不得降级为坏候选、多条独立局部搜索链、当前链实际轨迹反馈、损坏共享缓存、重复恢复执行者、崩溃预算耗尽后的状态发布、进程创建中断清理以及末轮状态标记；各修复均回归公开任务入口或必要的操作系统故障边界。另验证挂起原生批次在搜索期限内被终止。
- 默认真实入口：`tests/sim2gse/manual_search_task.py --output .local/sim2gse/implementation/任务03验收-20260912-r2`，退出码 0。累计 573.485 秒，入口墙钟 574.015 秒；187 个候选、666 个完整批次、四条局部搜索链共 11 轮；搜索按期限停止，末轮未完成标记为真。
- 五种节奏均完成最终 20 对独立批次，每种节奏每个对象 1,980 个有效样本；配对差值的 95% 区间下界均大于零。名义节奏相对有效初始序列提高约 60.6%，不代表超过自由选招参考或真实游戏收益。完整结果、验收摘要与候选文本保存在上述本地目录；游戏验收未执行。
- 保持同一工作树、`codex/sim2gse-implementation` 分支及固定基线 `254d2e3164490cb06ec9e1dad5fdefc71576ebcb`；正式规格的已跟踪差异与未跟踪检查均为空。未提交、推送、部署或执行游戏按键。
- 独立规范、规格和运行时审查完成，阻塞发现修复后定点复审均通过，无未解决的高、中优先级问题；最后的末轮标记修复覆盖剩余待处理候选与额度截断后列表为空两种情况。全仓正式构建与验证仍未通过，本票关闭仅表示第三票实现与针对性验收完成，不表示满足合并门禁。
