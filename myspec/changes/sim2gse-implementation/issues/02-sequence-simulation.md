# 同一导出序列的原生模拟

Status（状态）: closed
Triage（分拣）: ready-for-agent
Assignee（领取者）: unassigned

## Parent

[开发需求](../spec.md)

## What to build

将第一票生成并编译的同一序列经任务入口送入独立受控引擎，返回真实伤害、动作轨迹与编译一致性结果。沿用原生战斗机制，明确哪些结果是离线模型而非游戏证据。

## Blocked by

[真实角色到最小导出](01-character-and-export.md)

## Acceptance criteria

- [x] 从同一任务入口评估导出候选，实际消费的动作块、块内顺序和起始规则与导出编译结果相等，差异明确失败。
- [x] 单技能、不可用中间步骤、公共冷却连按、队列覆盖和同块动作均有原生执行轨迹，不能由自主选招补放。
- [x] 单主动与双主动物品遵守原生共享限制；不同顺序分别验证，测试装备只用独立样例，不修改用户角色。
- [x] 关闭控制器时与固定同版本原版回归一致；宠物、替换技能与被动效果保留原生来源。
- [x] SimC 实际执行的主动战前动作按原顺序进入同一 GSE 程序；重复动作不去重，战斗循环中的仅脱战步骤按空按键模拟。
- [x] 施法／引导等未支持方式明确拒绝，不能绕过限制或静默跳过；误传原版引擎说明不兼容。
- [x] 重复战斗的重置、起始相位和固定采样可复现，不复用上次战斗残留状态；用户实际安装继续后置。

## Testing Decisions

最高入口沿用第一票，输入实际产物并读取本次原生结果；覆盖 T04—T13、T15—T17、T21。参考[执行原型证据与交接矩阵](../../../../docs/sim2gse/execution-prototype.md)，把正式序列转换与原型自定义索引串区分开。主要成功与拒绝检查从任务入口运行，底层事件断言作补充。

## Specification coverage

`Sim2GSE evaluates the exported action order`、`Sim2GSE retains native combat behavior`；重置、输入与身份变更场景的执行基础。本票不重写上游被动机制，不增加完整搜索。

## Completion evidence

实现与测试证据见下方记录；游戏导入和按键验收仍由第五票处理。


### 2026-09-12 实施中

最高入口仍为 `run_task`（执行任务），新增可选命名动作块与起始相位用于验证；默认能力集合仍自动生成。同一候选通过固定上游编译后交给独立受控引擎，报告保留编译块、原生解析块、输入时间表和执行轨迹。多命令块仍限制单个公共冷却动作，并检查编译后宏的 255 字节上限。

已完成单技能接口缺失的红灯、真实角色全能力的原生崩溃定位、宏编译数值编号转名称差异及两个审查修复的红灯。灵界打击在未加控制器的原版同样崩溃；003 补丁只保护邪恶专精不存在的三项鲜血增益四处访问，独立审查确认不改公式。004 补丁增加输入时刻表用于相位与重置；005 补丁回报原生解析后的动作块，供逐项核对。

修复前一轮十二项入口检查、本机统一七项检查及真实角色替换技能冒烟通过；最后原生块回报和宏长度修复正在复测。成功快照 `.local/sim2gse/implementation/受控闭环 12/`，崩溃原始证据 `.local/sim2gse/implementation/debug-09/gdb.log` 与 `native-death-strike/`。尚不表示整票最终通过，也未执行实机验收。

### 2026-09-12 恢复第二票与针对性验收（中间记录）

用户明确继续第二票，全仓构建与验证错误留到 PR（拉取请求）合并前统一处理。本票没有放宽任何原有拒绝或事件断言，也未处理其他项目失败。

- `.venv/Scripts/python.exe -m unittest discover -s tests/sim2gse -p test_sequence.py`：7 项通过，34.557 秒；当前测试集中在根目录 `tests/sim2gse/`。
- 同一命令任务入口实际读取用户角色并成功返回 `offline_ready`（离线产物就绪）；证据目录 `.local/sim2gse/implementation/任务02验收-20260912/`，原始角色文件字节保持相等。
- `acceptance.json`（验收摘要）保存角色和候选散列。18 个导出编译动作块与原生解析回报相等；100 次请求、每次 180 秒，99 个有效统计样本。轨迹含 60000 次输入、15808 次派发和同数原生执行，以及 33 次队列覆盖。
- 最近完整验证记录 `.local/verification-audit/verify-84.log` 中，本项目 13 项通用测试和私人角色冒烟均通过；私人角色冒烟包含宠物结果及替换技能实际执行检查。该次全仓结果仍为失败，不能计作全仓通过。
- 本次仍在同一功能分支及固定基线开发，没有改写 `myspec/specs/` 正式规格。当时独立复审结论待补；该轮轨迹关联证据已由下方最终验收替代。

### 2026-09-12 第二票阶段验收完成

独立审查发现队列执行的来源编号丢失，旧检查按时间与动作关联，无法区分重复战斗。先在任务入口加入来源与步进断言，复现失败（3.312 秒），再用 006 补丁贯穿派发、队列重建和实际执行，保留原始输入编号；报告新增战斗编号与动作签名。按战斗、来源、步进、签名和时间逐项计数，派发与执行必须完全相等。修复后角色战斗结果和候选文本与修复前完全相同。

- 全部项目自动测试：`.venv/Scripts/python.exe -m unittest discover -s tests/sim2gse -p test_*.py`，13 项通过，42.713 秒。
- 实际命令入口和 `tests/sim2gse/manual_native_task.py`（真实角色冒烟检查）通过，覆盖宠物结果与替换技能实际执行。
- 最终证据：`.local/sim2gse/implementation/任务02验收-20260912-r2/acceptance.json`。输入字节保真，18 个编译动作块与原生解析相等，15808 次执行来源一致；每次战斗 180 秒，99 个有效样本。
- 独立复审：006 补丁、队列重排来源保留、双向轨迹计数和锁定散列未发现一级或二级阻断问题；针对性动态复测全部通过。

本票按上述离线范围关闭。全仓构建与验证失败及正式干净工作树门禁按用户安排留到 PR（拉取请求）合并前处理；游戏验收仍为未执行，归第五票。搜索优化归第三票。

### 2026-09-13 战前动作与执行来源补充

基准报告新增原生战前动作定义；能力目录只采用 SimC 实际执行且可映射为玩家按钮的动作，并保持 `action_sequence_precombat`（战前动作序列）的顺序和重复项。导出使用 `[nocombat]`（仅脱战）条件；受控引擎在战斗循环中保留这些步骤的位置但不施放技能，并逐场核对原生战前执行记录。60 项 Sim2GSE 集中测试通过，两套固定源码补丁链均从上游归档重新应用成功；游戏内战前按键验收仍归第五票。
