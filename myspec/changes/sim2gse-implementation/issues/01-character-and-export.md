# 真实角色到最小导出

Status（状态）: closed
Triage（分拣）: ready-for-agent
Assignee（领取者）: unassigned

## Parent

[开发需求](../spec.md)

## What to build

从单个真实角色导出进入同一本地任务入口，自动取得角色与可按动作，形成简单合法序列并生成可核查的离线导入文本。输入、动作集合、原生参考和导出结果可由一次任务追溯，不依赖人工白名单或编辑研究脚本。

## Blocked by

无其他实施票；仍需开发流程的干净工作起点、必要工具可调用与“开始开发”授权。

## Acceptance criteria

- [x] 从声明的任务入口输入用户真实角色，当前装备、天赋、中文和注释保真，背包候选不替换已装备物品。
- [x] 自动识别统一主动动作，包含已支持的主动物品；被动效果保留原生处理，首版无药剂选择。
- [x] 多角色、外部引用、输出覆盖、不支持专精与主动动作在模拟前明确报错；不静默删除能力。
- [x] 固定身份的独立原生引擎产生真实参考结果；简单候选完成真实编码往返和上游编译入口校验，不假称已通过游戏导入。
- [x] 入口可从中文空格路径读入真实文件，导出报告区分参考成绩与未受控模拟的候选，未通过检查不输出假字符串。
- [x] 接入本项目必要构建与校验，不安装无关工具，不修改配装器；把原型中的固定样例与生产识别规则分开。

## Testing Decisions

最高入口：拟建的本地任务公开入口，输入角色后返回任务结果及失败原因；本票必须提供可运行调用方法，供后四票原样延伸使用。先从该入口验证缺失行为，再实现；覆盖 T01—T03、T14、T21。底层参考[动作规则](../../../../docs/sim2gse/action-capabilities.md)、[离线导出原型](../../../../docs/sim2gse/export-prototype.md)，不能把历史原型断言当成本票通过。

## Specification coverage

`sim2gse-character-input` 全部需求；`Sim2GSE reveals only available export results` 的编码失败场景。本票仅交付最小离线导出，不实现搜索，也不要求安装游戏插件。

## Completion evidence

以下记录实施过程，本票尚未验收完成。

### 2026-09-12 实施中

本次用户已明确授权开始开发。同一工作树 `D:/My Project/Wow Addons`，功能分支 `codex/sim2gse-implementation`，干净起点及固定基线 `254d2e3164490cb06ec9e1dad5fdefc71576ebcb`。沿用已确认的五票顺序及同一任务入口，采用完整流程。只读架构角色已复核主动来源、原生映射和可复用接缝。

当前入口为 `scripts/dev/sim2gse/task.py <角色文件> --output <新任务目录>`，产品实现位于 `projects/sim2gse/`。已取得入口仅解析时的失败检查、真实引擎路径错误和饰品指针错误的失败证据，并完成对应修复；当前人工样例五项检查与真实角色冒烟通过。真实角色参考为 99 个有效样本，原生模型提示保留；候选编译通过但尚未受控模拟及游戏验证。

本票未标记完成，仍需统一验证、能力完整性审查及必要修复。主规格未改，后续票未开始，未执行远端交付。

## Comments（讨论）

2026-09-12 用户确认：明确排除纯移动、控制和辅助技能；有伤害的能力仍须支持或报错。分类须自动、有来源，不以原生缺少实现作为排除依据。

独立审查发现能力静默删除、装备仅核对编号及部分失败未形成报告。已移除无映射/原生禁用能力的继续执行路径，装备改为核对所有声明字段，预期依赖及子进程失败归一化为失败结果。新的真实入口记录在 `.local/sim2gse/implementation/严格核对 03/`：主动能力存在九项缺口，模拟与导出停止。此前成功记录仅为开发阶段证据，不能用于本票验收。当前五项公开入口检查验证严格拒绝及输入保留；真实成功冒烟仍未通过，正在按用户澄清处理分类。


2026-09-12 复核修正：先前将未知服务器脚本视为一律阻塞，是实施自加的过严门槛。按用户用途边界及设计已有兼容性证据机制，四项辅助能力采用固定原生说明的分类补充；自动发现仍覆盖全部当前能力，证据版本或散列变化失败，未分类能力不放行。真实入口 `.local/sim2gse/implementation/角色闭环 08/` 已成功，候选仅标离线编译通过，未受控模拟、未游戏验证。六项公开入口检查通过，含未知种族能力拒绝；真实角色成功冒烟通过。独立产品引擎构建通过，本机常规统一验证七项通过；尚未执行固定基线的干净正式验证及远端检查。

第一票阶段验收完成：独立审查的三项发现及最后分类字段校验均已修复，六项入口检查复跑通过。后续正式干净基线验证、整体审查及游戏验收仍按开发流程执行，不以本票阶段验收替代。
