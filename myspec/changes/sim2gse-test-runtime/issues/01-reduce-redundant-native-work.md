# 01: 减少测试中的重复原生工作

**What to build:** 在不改变产品行为、模拟预算和覆盖含义的前提下，让搜索状态、缓存、恢复、结果发布与职业兼容测试只运行其断言必需的真实层级；复用序列准备，同时保留完整浏览器、完整搜索复测、原生序列和 Windows（操作系统）边界证据。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [x] 搜索反馈、缓存损坏、预算恢复和候选选择使用确定性构造输入，并仍经过对应真实协调逻辑。
- [x] 相同角色的序列测试不重复无关的基准或受控模拟，需要原生轨迹的断言继续运行真实引擎。
- [x] 职业兼容场景不重复整套浏览器与搜索流程，同时保留至少一个完整浏览器真实任务和一个完整搜索复测入口。
- [x] Windows 进程树、句柄、超时和文件共享检查继续使用真实操作系统边界。
- [x] 相同本机 8 进程命令完整通过，并记录修改前后耗时与测试清单对应关系。
- [ ] PR（拉取请求）全部必需检查完整通过；从 Runner（运行机器）启动到通过的墙钟时间不超过 180 秒。

## Comments

- 2026-09-15：用户确认先从测试内部简化，不先增加外部 Runner（运行机器）分片。
- 2026-09-15：本机相同 8 进程命令从 38.32 秒降至 27.49 秒，70 项测试和 31 项子测试全部通过；构造边界最终仅替换 `sequence.evaluate`（序列评估），复用当次真实原生参考报告。
- 2026-09-15：PR #13 首次远端验证虽全部通过，但 Windows 必需检查耗时 5 分 58 秒；日志确认工作流仍把仓库配置的 8 进程临时改为 1，导致 SIM2GSE 串行耗时 207.07 秒。删除该历史临时覆盖，继续使用统一配置，不新增 Runner 分片。
