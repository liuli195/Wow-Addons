# 01：复测未完成时导出锁定候选

Label（标签）: change:task
Triage（分拣）: ready-for-agent
Status（状态）: closed

## What to build

当搜索已经锁定候选、但最终复测因预算未完成时，任务入口导出锁定候选并保留证据不足提示；最终复测完整但未证明改善时仍导出初始序列。

## Blocked by

无，可立即实施。

## Acceptance criteria

- [x] 公开任务入口的失败检查能够复现复测未完成时错误导出初始序列。
- [x] 修复后同一检查导出锁定候选，且状态仍为复测不完整、不能视为验证通过。
- [x] 完整复测未证明改善时仍保留初始序列。
- [x] 使用原始角色输入和默认 SimulationCraft 参数完成一次正式重跑，万奥宝典字段保持不变。

## Completion evidence

- 旧实现下公开入口检查失败：选用编号为初始序列，和锁定编号不同；修复后同一检查通过。
- Sim2GSE（角色级按键序列优化器）测试共 68 项通过。
- 默认重跑输出：`.local/sim2gse/default-rerun-20260915-045302/result.json`。
- 重跑状态为完整验证完成，五个最终复测场景均确认改善；锁定编号与选用编号同为 `b24b86b665c2cdbddb4519de54c8174d34b85e184b92cd2f9c31f5b171f456be`。
- 原始输入和规范化输入均保留 `omnium_talents=136814:1/136821:1/136817:1/136819:1/136822:1`，未传入额外搜索参数。
