# 01: 采集真实 GSE 输入与施法时机

**What to build:** 提供一个可在 12.1 正式服独立启用的 Sim2GSE 时机探针；用户通过斜杠命令开始、标记和停止会话，插件把 GSE 动作提交及玩家原生施法事件以毫秒时间写入 SavedVariables，且不修改 GSE 或自动产生输入。

**Blocked by:** None (can start immediately).

**Status:** closed

- [x] `/s2gprobe start|mark|stop|status` 可通过同一个插件入口完成完整采集流程。
- [x] GSE 点击记录包含可复核的按钮、点击序号、提交步骤、动作和触发边沿。
- [x] 玩家施法事件记录包含事件名、`castGUID`、技能编号和毫秒时间。
- [x] 会话条件包含法术队列窗口、按下触发设置、网络延迟、符文和符文能量；受限值安全降级。
- [x] 离线公开接缝测试先失败后通过，并明确保留实机验收边界。

## Comments

- 2026-09-15：用户确认开始开发独立探针插件；不修改 GSE，不包含自动输入。

## Completion evidence

- 红灯：公开入口测试因探针插件不存在而失败；随后按同一入口完成绿灯。
- 探针公开入口测试通过：1 项；Sim2GSE 全组通过：71 项及 31 项子测试。
- TOC（插件清单）、Lua 5.1 语法、Luacheck（静态检查）和 LuaLS（语言检查）均通过。
- 固定基线快速验证状态为 `passed`，20 个匹配检查全部通过，耗时约 29 秒。
- 尚未部署到游戏目录或执行真实客户端验收；离线结果不作为法术队列行为证据。
