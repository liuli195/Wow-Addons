# 02: 采集按下触发的 GSE 中继输入

**What to build:** 使用按下触发的 GSE 绑定时，探针从真实收到硬件输入的中继路径记录完整点击，让保存结果包含序列、提交步骤、动作和触发边沿，而不只记录原生施法事件。

**Blocked by:** 01/采集真实 GSE 输入与施法时机（已完成）。

**Status:** ready-for-human

- [x] 离线公开接缝复现中继按钮转发路径，并能在修复前得到零点击的失败结果。
- [x] 修复后同一路径只记录一次完整点击，触发边沿为按下中继。
- [x] 既有直接执行按钮路径与受限值安全降级继续通过。
- [ ] 部署后由 12.1 正式服确认按键会增加点击记录；五段队列实验在此之后重跑。

## Completion evidence

- 红灯：真实中继路径断言失败，提示 `keydown relay has no observable completion hook`。
- 绿灯：同一公开接缝测试通过，记录一条完整点击；直接执行按钮路径和受限值降级保持通过。
- 固定基线快速验证状态为 `passed`，检查插件语言、静态规则、清单、运行测试、文档和测试登记。
- 0.1.1 已部署且与仓库源码文件指纹一致；正式服按键采集等待人工验收。
