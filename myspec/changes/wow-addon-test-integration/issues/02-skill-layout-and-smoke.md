# 02: 自包含技能布局与双代理冒烟

**What to build:** 工具成为**一个自包含技能**——仓库内装好即可被两个代理发现并使用，
不需要定位指针、安装器或回链；数据以压缩形式随技能分发且**只有工具消费**；
面向代理的输出走**统一有界出口**。

**Blocked by:** 01（契约与来源核对）

**Status:** 本地技术验收完成——Codex 技能入口冒烟已补证；两个代理的完整任务履约由 ChatGPT 统一验收，远端 CI 仍归票据 05

- [x] 技能真身位于仓库技能目录，另一代理侧为**目录连接**；**不依赖**定位指针、安装器、工具原位置回链
- [x] 技能目录组织符合共享规范：入口 / 参考文档 / 脚本 / 其余载荷
- [x] 入口文档**不出现数据文件路径**；路径以**相对技能根**表述
- [x] 数据以压缩形式存放，工具内部解码；**正常运行前不要求代理手工解压**
- [x] 面向代理的输出走统一出口并受限额：总字节上限、差异预览条数上限、**保留真实退出状态、完整计数与"详情已截断"标记**
- [x] **截断的是展示，不是实际执行的用例数**
- [x] **G2**：Codex 技能入口已实际调用，Claude 侧目录连接与共享真身已验证；完整双代理任务履约由 ChatGPT 统一验收
- [x] 布局重排后工具自带自检**仍全部通过**
- [x] 技能内部自检**在根目录显式登记调用入口**，不复制两套实现

## 完成证据

技能真身 .agents/skills/ + Claude 侧连接；压缩载荷（12MB→429KB）；打包不变量经反证。提交 2cdab8b、fe71475。

**复审后订正（提交 5296d15 起）**

- 自检项数：**38**（原记 32，此后又加了数据规则、压缩资产时间戳、时序行为探针及判定失败保护等项）。
- 输出限额：原实现**裁剪一次不保证字节数**——字典键数不受条数限制约束，100 个字段 × 512 字符约 52 KB，超出 16 KB 预算。已改为逐级收紧到真的装下，并补上"字段多""宽嵌套""预览条数""失败信息上限"四条边界用例。
- 会超预算的命令原先**没有取回完整结果的路**（catalog 把 40 个专精截到 10 个就没了）。已补 `--output`。
- 措辞订正：本条验收条件里的"无安装器"指的是**不需要**安装器（技能自包含、原地可用），不是"不许存在"。`install-skill` 把整份技能复制到**另一个项目的**技能目录，供后续复用；已补测试证明它只往目标项目里写、且副本能独立跑。

**此前未验证**：Codex 侧的实际加载与完整任务冒烟——上次因用量上限阻塞，之后补证前只有目录结构证据。

**本轮补证（提交 1d07f92 后）**：当前 Codex 会话已从仓库技能目录发现并读取
`.agents/skills/wow-addon-test/SKILL.md`，使用同一技能入口并固定仓库 Lua 5.1 执行
`doctor` 与 MYUI 死亡骑士运行；报告为 78 个用例、141 个检查点、72 个普通通过、6 个已批准已知差异、
0 个未登记差异、0 个执行错误。两个代理的完整任务履约仍由 ChatGPT 按本变更约定统一验收，
不再重复派发两套完整验收。
