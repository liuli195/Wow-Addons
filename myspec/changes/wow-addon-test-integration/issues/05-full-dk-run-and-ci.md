# 05: 死亡骑士全量核对与真实 CI

**What to build:** 在**锁定数据**上跑通死亡骑士三专精 × 三组件，**逐条核对**；
差异按"通过 + 已知差异"如实标注；**远端 CI 真实执行**。

**Blocked by:** 03（真实三组件接入与双边投影）、04（数据检查规则、时序口径与清理字段）

**Status:** done

- [ ] **逐条核对**用例、检查点、组件（不只看总数）；**空选择不能通过**；未选择的专精不计入通过
- [ ] **已知差异**按事先确认的**条件、组件、字段**登记；**不符合该产品期望的仍判失败**；
      成绩单由实际结果产生，**不预先锁死差异条数**
- [ ] **真实缺陷注入**被检出（含恢复后重新通过），且不以语法错误、缺依赖或超时冒充
- [ ] **G4** 缺接口／缺输入**显式失败**，不落入合法空状态
- [ ] **G5** 干净克隆后工具摘要**全部匹配**
- [ ] **G8** 连跑两次**受跟踪文件不变**
- [ ] **G9** 压缩载荷在技能目录下**实际可用**
- [ ] CI **真实执行**并通过；触发路径与缓存输入已登记；本机预算与远端超时**分别报告**
- [ ] 既有准星 HUD 回归（可见性、读数、配置）**全部保留且通过**

## 完成证据

逐条核对/空选择/缺读数/重复运行均入测试；G9 载荷可用；G8 通过。提交 3d9b7c2。

**复审后订正（提交 5296d15 起）**

- 统一入口完整验证通过：**45.66 秒 / 60 秒预算**，37 项检查全过；两条本技能的检查 `verify.wowaddontest-skill`（8.55s）与 `verify.wowaddontest-dk`（10.31s）分别计时。
- 工具自带自检**已接进统一入口**，并固定用仓库自带的 Lua 5.1（原来只是计划里的记录，没有接线）。当前 37/37。
- **仍未闭合**：真实 CI 尚未执行——分支未推送，PR 未创建，所以"远端 CI 通过"目前没有证据，不能算完成。
