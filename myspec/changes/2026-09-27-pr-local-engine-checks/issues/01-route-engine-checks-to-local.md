# 01: 将引擎检查限定在本机

**What to build（交付结果）：** PR 使用统一构建与验证配置时跳过引擎构建和 Sim2GSE 引擎测试，保留其他现有检查；本机完整构建验证仍覆盖全部原有目标。

**Blocked by（阻塞任务）：** None（无，可直接开始）

**Status（状态）：** completed

- [x] PR 构建和完整验证明确报告引擎检查被排除，其余可执行检查通过。
- [x] PR 的固定基线快速验证也按同一场景选择，不误跑引擎检查。
- [x] CI 不再准备、编译或缓存引擎；本机运行 PR 准备入口通过，远端实际结果在交付阶段核验。
- [x] 本机不带 PR 参数时，两个构建目标和全部 37 项验证检查仍运行并通过。
- [x] 正式规格更新预览与仓库说明准确区分本机和 PR 的检查范围。
- [x] 回归检查不再把 Sim2GSE 自动发现测试误称为 PR 默认覆盖。

## Comments（讨论）

- 2026-09-27：用户确认只标记现有检查，不将依赖私人资料的手工验收纳入统一配置。
- 2026-09-27：本机与 PR 场景的红绿实测见同目录 `verification.md`。正式规格仍按开发流程留待门禁二通过后应用。
- 2026-09-27：固定基线验证已完成，本机 37 项、PR 36 项均通过。MySpec 预览通过结构校验；远端实际结果仍留待交付阶段。
