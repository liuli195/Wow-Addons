# 确认首轮角色、场景与输入方式

Label（标签）: wayfinder:grilling
Triage（分拣）: ready-for-human
Status（状态）: closed
Assignee（领取者）: Codex（主代理）
Mode（方式）: HITL（与用户共同决策）
Parent（所属地图）: [Sim2GSE（角色级按键序列优化器）实施地图](../spec.md)
Blocked by（前置事项）: 无

## Question（问题）

第一轮使用哪个专精及其角色样本，最小可行版本覆盖什么目标场景，采用怎样的名义输入间隔？哪些实际配置与能力还要在后续取证？

## 调查或讨论范围

- 区分当前角色、已有历史样本和上游测试配置；本地能查到的资料由代理先查，不能要求用户重找。
- 与用户明确首轮场景及名义输入间隔；实际插件安装、按键和重置配置等到产出候选结果、进入真实导入验证时再讨论。
- 范围以本票最新用户决议为准，不把原方案的鲜血、双场景或 200 毫秒示例继续作为当前要求；具体动作取舍由支持范围票承载。

## 关闭条件

- 用户明确首轮对象和测试条件，并说明哪些仍是未校准假设。
- 形成获取证据所需的最小清单；不把确定研究范围写成游戏验收通过。

## 实施交接

P0（兼容性核实）的角色与场景输入。

原始依据：[实施方案](../../../../docs/Sim2GSE_Implementation_Plan.md)第 3、5、7.5、14.2、15.2 节。关闭本票只代表所问问题有证据或已作决定，不替代后续完整产品验收。

## Comments（讨论）

2026-09-12：用户要求进入任务，主代理已领取。先只读核对现有角色样本与版本资料，再与用户确认首轮验证条件；本票保持开放，尚未形成决议。

### 2026-09-12 首轮事实核对

- 已找到两份历史用户配置，均为 90 级、高岭牛头人、鲜血死亡骑士；较新一份位于 `.local/gear-planner-research/cn-user-20260909-0349.input.simc`。文件说明已省略身份并规范化转义，属于历史整理样本，不冒充今天未经处理的原始导出。
- 较新样本的 SHA-256（文件散列）为 `d29a102d281d83dca872d980e58a6f583a052ada6d7580f395701243f910222b`；主代理检查未发现动作列表覆盖、迭代数或战斗时长设置。两件饰品的记录存在，但完整效果支持尚未核实，留给支持范围票。
- 既有死亡骑士参考验证另有属性快照／等待输入，不能拿其成功结果代替正常战斗基线。
- 本机注册信息指向 `D:/Program Files/World of Warcraft/_retail_/`；该目录的 `Wow.exe`（游戏程序）文件版本为 `12.1.0.69587`，与仓库锁定值相同。这是磁盘程序证据，本次未启动游戏核对运行时输出。
- 在该安装目录未找到标准 GSE（按键序列插件）目录、名称相关插件目录或账号级 GSE 保存变量文件；没有据此推断另一份安装的状态，也未安装或修改任何游戏文件。

### 当时提出的建议（已由下方用户决议替代）

1. 首轮先用上述历史样本，正式验收前更新为当前角色导出；或先取得新导出再开始。
2. 先静止单目标、再固定 5 目标；或调换顺序。两类场景均保留。
3. 先用每 200 毫秒一次有效输入、每次测试从第一步开始的可调假设，随后按实际绑定与重置配置校准；或先取得真实配置再开始。

以上是先前提出的建议，不是最终采用方案。具体合法技能及主动饰品支持清单由[确定首版动作与饰品支持范围](06-supported-actions.md)承载。

### 2026-09-12 用户决议与关闭记录

1. **专精与样本**：改为邪恶死亡骑士，用户将提供样本。不使用鲜血死亡骑士，已有鲜血历史配置不作为本次角色替代品；不据此假定邪恶角色也是 90 级或具有相同装备。
2. **最小范围**：先只做静止单目标，打通最小可行闭环。固定多目标移到后续扩展，不再作为本次首版完成门槛。
3. **名义输入间隔**：300 毫秒，约每秒 3.33 次有效输入，作为当前选定的模型参数。它不是已测得的真实按键轨迹；公共冷却期间仍按既定输入与步进语义处理，不能改成每个公共冷却只推进一次。

最小待取证清单：用户邪恶专精的完整 `/simc` 导出、目标游戏和 GSE（按键序列插件）实际版本、最小普通序列导出、实际绑定及跨战斗重置配置。按下／松开和重置行为尚未由本次答复确定，保留到[取得目标版本、角色与最小序列样本](05-target-evidence.md)，不把先前建议的“每次从第一步开始”写成已获实机证明的配置。

本票解决了首轮对象、场景与名义输入选择，现已关闭。角色文件尚未收到，原生基线、支持范围和游戏验收均未通过本轮执行；后续取证票继续开放。活动计划已按本决议同步。

### 2026-09-12 用户补充与顺序纠正（当前有效）

- **角色样本已收到**：用户提供 02:40 导出的 90 级邪恶死亡骑士角色，声明游戏构建 `12.1.0.69587`、接口 `120100`。原文保存于 `.local/sim2gse/target-evidence/unholy-20260912-0240.chat.txt`，散列为 `3d0ff45e396f3f696c2c4e9b4c7eb4442b3998b2e88b27465751e9776fcf3f08`；仅还原聊天下划线转义的运行副本同目录保存，天赋和装备保持用户输入。本票只接收样本作为首轮范围依据。
- **范围与节奏保持不变**：邪恶死亡骑士、静止单目标、300 毫秒名义输入间隔；优先最小可行闭环。
- **安装后置**：用户明确要求先产出离线候选模拟结果，再走到真实导入验证阶段，到时才讨论 GSE（按键序列插件）安装及实际配置。不得提前询问安装或以未安装阻塞前面的任务，也不把原生参考数值当成受控序列结果。
- **严格按地图顺序**：用户明确当前仍在本票。先完成当前任务，再按地图既定顺序处理后续事项；不能因为样本到达、依赖已满足或代理可并行，就跳到后续任务。早先已完成的背景研究保留，但不因此自动领取后续事项。
- **越序纠正**：主代理在本轮提前运行原生参考并领取、关闭了后续样本票，用户指出越界。现撤销后续票的领取和关闭，撤销新拆出的导入验证票；提前产生的本地报告仅留作将来复核材料，不计入后续任务完成状态，也不代表支持清单或候选序列已通过。

首轮范围决议已补齐，本轮到此收尾；不自动推进下一个任务。后续票据中旧的“提前取得插件安装／实际配置”前置要求服从本节用户决定，按顺序处理到对应离线部分时再细化，真实导入验收要求保留到候选结果产出后。
