# 验证最小序列的导入与编译契约

Label（标签）: wayfinder:prototype
Triage（分拣）: ready-for-agent
Status（状态）: closed
Assignee（领取者）: Codex（主代理）
Mode（方式）: HITL（原型与用户实际导入）
Parent（所属地图）: [Sim2GSE（角色级按键序列优化器）实施地图](../spec.md)
Blocked by（前置事项）: [确认项目目录与上游补丁维护边界](04-project-boundary.md)；[确定首版动作与饰品支持范围](06-supported-actions.md)

## Question（问题）

统一动作块表示怎样才能被目标版本导入并编译成相同顺序的命令？最小往返原型是否支持把这份契约交给正式开发？

## 调查或讨论范围

- 按一个技能、重复技能、中文说明、多命令块、槽位 13/14 的小样本提高保真度，不先搭建完整优化器。
- 核对对象外壳、表类型、元数据、法术映射、压缩封装、长度、初始位置和重置行为；字符串无需逐字一致，核心语义必须一致。
- 本票先执行离线原型；真实导入、查看客户端编译结果并再次导出按既有用户决议延至候选模拟结果产出后。分别链接证据，不能以离线替身或代理自评代替真实导入与用户反馈。

## 关闭条件

- 代表性最小样本分别记录本地往返、目标导入和编译一致三层结果；T14—T16 的完整矩阵作为后续正式实现要求，不在此票一次铺开。
- 有用户反馈及明确契约；游戏行为尚未证明的部分交给执行模型票，不误报全部通过。

## 实施交接

P1 的导出契约；正式实现仍通过开发流程交付。

原始依据：[实施方案](../../../../docs/Sim2GSE_Implementation_Plan.md)第 6、12、15.3、16.1—16.2 节。关闭本票只代表所问问题有证据或已作决定，不替代后续完整产品验收。

## Comments（讨论）

尚未领取；结论在本节追加，不提前填入答案。

### 2026-09-12 按顺序领取

用户要求进入本票，并询问“契约”的含义。此处指生成器与 GSE（按键序列插件）之间的数据格式、导入解释及编译顺序约定。先做少量代表性样本的离线编码、解码与固定源码编译核对；依据已有安装后置决议，当前不要求真实游戏导入，不将离线结果写成实机验收。本票保持开放，实际发现及验证边界在下文追加。

### 2026-09-12 首轮离线原型结果

已建立[最小序列原型及结果说明](../../../../docs/sim2gse/export-prototype.md)，复用固定提交的上游测试替身、独立 CBOR 解码器、导入分支、校验算法和模板编译器。六种样本各覆盖单序列及集合外壳，共 12 例通过；八个 CBOR 编码向量通过，校验字段篡改与原型超长宏拒绝检查通过。第一次原型运行因未执行上游延迟初始化而失败，按作者测试加载器的方式补齐后通过，未修改上游源码。

通过层为本地压缩往返、独立解码对象一致、解码后上游导入分支及编译顺序。测试替身涉及客户端法术信息、资料片编号及存储边界；真实压缩封装、前缀分隔符容忍、客户端导入和重置行为未验证。`.candidate.txt` 只表示待验证字符串，不能交付为已确认可用的推荐序列。

源码散列、每例轨迹和可双击浏览步骤的演示位于 `.local/sim2gse/export-prototype/`。实际游戏导入继续在候选模拟结果之后进行，本轮不要求安装，不进入下一项；任务保持开放。

### 2026-09-12 用户确认结项

用户明确要求“标记已完成，然后进入下一步”。本票按离线数据格式、解码后导入分支与编译顺序的原型证据关闭，进入执行模型票。真实客户端编码封装、字符串导入、重置及游戏行为仍未验证，沿用候选模拟结果产出后的验收安排；关闭规划票不代表这些验收已通过。
