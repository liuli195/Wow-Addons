# 取得目标版本、角色与最小序列样本

Label（标签）: wayfinder:task
Triage（分拣）: ready-for-agent
Status（状态）: closed
Assignee（领取者）: Codex（主代理）
Mode（方式）: AFK（用户样本已提供，代理完成本票离线取证）
Parent（所属地图）: [Sim2GSE（角色级按键序列优化器）实施地图](../spec.md)
Blocked by（前置事项）: [确认首轮角色、场景与输入方式](01-first-validation-scope.md)；[核实引擎可复用基础及按键控制入口](02-engine-evidence.md)；[核实序列编码、编译与按键语义](03-gse-evidence.md)；[确认项目目录与上游补丁维护边界](04-project-boundary.md)

## Question（问题）

为邪恶死亡骑士单目标最小闭环确定支持范围和编码契约前，如何取得彼此匹配的目标版本、完整角色资料、原生基线及序列格式依据？实际游戏样本按首轮用户决议延后至有候选结果的导入阶段。

## 调查或讨论范围

- 核对样本中的客户端构建号、专精、角色、天赋与装备，保留完整 /simc 导出。
- 名义输入间隔已选定 300 毫秒，不要求用户重新决定。实际插件版本及按键／重置配置按用户要求延后至真实导入阶段，不作为原生取证的前置条件。
- 角色文件默认保存在本地运行目录；记录散列与来源，不把私人原始导出附进任务文件。
- 运行匹配原生引擎，保留退出码、输出、角色与关键装备支持情况；不调用计划中尚不存在的项目命令。
- 缺当前资料可继续标明测试配置的独立工作；该票的目标角色证据不能由历史样本或等待时间替代。

## 关闭条件

- 版本、角色与原生基线可相互对应，有来源和实际运行证据。
- 现有模型提示明确记录并交给支持清单处理；运行成功不等于所有效果、按键模型或游戏行为通过。

## 实施交接

P0 的实际兼容性依据。

原始依据：[实施方案](../../../../docs/Sim2GSE_Implementation_Plan.md)第 5.3、12.3、15.2、16.2 节。关闭本票只代表所问问题有证据或已作决定，不替代后续完整产品验收。

## Comments（讨论）

尚未领取。2026-09-12：首轮范围票已关闭，前置研究也已完成；本票现在可处理。用户邪恶专精样本、实际版本与序列证据尚未取得，不能视为已验收。

2026-09-12：主代理领取。用户已在会话提供邪恶死亡骑士的完整角色导出文本；开始保存会话原文、核对转义与版本，并使用已有原生程序取得单目标兼容性证据。真实序列与绑定配置仍待补齐，票据保持开放。

2026-09-12：已保存原文和仅还原下划线转义的运行副本，实际运行固定原生引擎的单目标战斗；版本、天赋与报告中的装备编号核对通过。完整来源、散列、参数、日志和限制见[邪恶角色样本与原生单目标取证](../../../../docs/sim2gse/target-evidence.md)。现有证据证明角色可执行战斗，但报告有符文模型未验证提示，需留给支持范围审查；不能判定全部效果或 GSE（按键序列插件）模型通过。

当前精确缺口：实际 GSE（按键序列插件）版本及最小导出、按键／队列／重置配置、对应游戏内行为证据。已询问用户安装状态，未得到答复前不选定实际版本或生成虚构样本。本票保持开放。

### 已撤销的越序关闭记录 — 2026-09-12

主代理曾在尚未轮到本票时，以角色原文和原生运行记录为依据将本票关闭，并新拆导入验证票。用户明确当前仍在首轮范围票，要求严格按地图顺序。上述领取、关闭和新拆票已撤销。

本票恢复开放、未领取；已有本地运行记录仅留作将来复核，不计为本票完成，不解锁动作支持票。安装及实际游戏配置的时机以[当前首轮范围决议](01-first-validation-scope.md)为准；轮到本票时只处理其离线部分，不要求提前安装插件。

### 2026-09-12 按顺序正式领取

前四项已完成，用户现在要求进入下一项。主代理正式领取本票，只处理已提供邪恶角色的离线取证：复核完整输入、为序列优化器准备独立的官方原版参考程序、取得单目标原生基线并记录模型提示。本项目程序不得指向配装器的可执行文件或改版引擎；按键控制补丁、动作支持决策和实际游戏导入分别留到对应后续票据。

此前安装／真实游戏配置缺口已由首轮用户决议明确后置，不能继续作为本票关闭条件。序列格式依据优先沿用已完成的固定版本研究；本票不伪造用户导出样本，不提前制作编码或执行模型原型。旧运行资料仅作复核和对照，新证据单独保存。

### 2026-09-12 按顺序完成

已复核用户邪恶角色原文与仅还原下划线转义的运行副本；为本项目复制独立的官方原版参考程序，文件身份独立，来源散列核对通过，未混用配装器改版。以该程序实际完成一次 180 秒静止单目标原生基线，退出码为 0，报告版本、角色、天赋及 15 个装备槽位的物品编号核对通过，平均约 79337.48 DPS（每秒伤害）。新报告、日志及来源记录见[邪恶角色样本与原生单目标取证](../../../../docs/sim2gse/target-evidence.md)，旧资料只作对照。

主手非天启符文提示与符文效果“实现尚未经验证”提示均原样保留，交给下一项支持范围审查；上述原生数值不是 300 毫秒序列成绩。序列格式依据沿用已完成的固定版本研究，实际插件版本、导出样本和游戏配置按用户决议后置。当前离线关闭条件已满足，本票关闭；未领取或推进下一项。
