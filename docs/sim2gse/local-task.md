# 本地任务入口

当前三步界面与命令入口共用真实计算程序，默认自动执行搜索、独立复测与离线导出。这个入口读取单角色并先运行固定引擎的自由选招基准，再依据整批执行计数和原生动作标记取得可按动作，生成离线序列，经固定 GSE（按键序列插件）源码编译核对后，将同一动作块交给受控引擎评估。

职业、专精和职责交给原生引擎判断，坦克与治疗也沿用伤害优化目标。导出编号取原生角色身份；种族别名由引擎解析，原文保留。受控模拟保留原生战前准备，施法、引导、蓄力及派生效果使用原生调度；不再固定邪恶专精起手。基准的装备组代理逐项记录实际使用后再转换，不因其后台标记而遗漏饰品。

基准中由原生增益明确标为同一基础按钮的替换形态合并到一个动作块，由原生就绪状态决定实际执行哪一形态。当前已验证狂暴战士的怒击替换关系；这不是完整的游戏按键兼容证明，未实例化为增益的形态被动及游戏内蓄力操作仍需后续实际验收。

## 三步界面

在仓库根目录启动，然后用浏览器打开终端显示的本机地址：

```powershell
.venv/Scripts/python.exe scripts/dev/sim2gse/interface.py
```

默认地址为 `http://127.0.0.1:8780/`；端口被占用时使用 `--port 0` 自动选择空闲端口。粘贴角色导出文本并点击“开始运行”，页面显示真实阶段、已完成批次和累计用时；结果就绪后才显示复制区。修改输入或重新开始会清空旧结果，短暂断连会继续等待同一任务。

界面仅绑定本机地址，角色和报告保存到 `.local/sim2gse/ui-tasks/`，可通过 `--output-root '中文 空格目录'` 指定位置。复测不足、未证明改善与运行失败分别说明；复制不会清除证据不足提示。游戏内导入仍未验收。停止服务使用终端的 Ctrl+C（中断快捷键）；重新启动服务后，未完成任务可按下方内部命令恢复，日常页面不增加取消、恢复或调试按钮。

## 单个文件入口

在仓库根目录运行，输出必须是尚不存在的新目录：

```powershell
.venv/Scripts/python.exe scripts/dev/sim2gse/task.py '角色文件.simc' --output '.local/sim2gse/新任务'
```

产品代码位于 `projects/sim2gse/`，命令入口与后续界面共用 `run_task`（执行任务）。角色原始字节、规范化运行文本及原生报告保留在任务目录。`result.json`（任务结果）保存能力来源、自由选招参考、搜索记录、候选和逐情景复测结果；`progress.json`（进度）报告当前阶段、累计耗时和完成批次。`candidate.txt`（候选导入文本）对应最终选用序列；复测未完成时明确标为证据不足，不标为已验证。失败不会覆盖旧任务。

## 取消与恢复

同一任务模块提供 `start_task`（启动）、`cancel_task`（取消）、`resume_task`（恢复）和 `read_task`（读取）入口，供后续界面共用。取消立即停止派发并清理所属进程树，完整批次留在 SQLite（本地数据库）。任务正在运行时拒绝第二个执行者恢复。

```powershell
.venv/Scripts/python.exe scripts/dev/sim2gse/task.py --resume --output '.local/sim2gse/已有任务'
```

恢复沿用原有配置、随机数、候选锁定和累计预算；版本或输入变化须新建任务。最终候选锁定后只继续复测。详细预算、缓存与五种节奏见[运行契约](runtime-contract.md)；最终目标是每种节奏、每个对象 20 个独立批次，每批请求 100 场、实际有效 99 场。未完成目标不会自动延长十分钟上限。

## 当前准备方式

复用仓库 `.venv`（解释器环境）。唯一新增依赖为项目要求文件中的固定 CBOR（紧凑对象编码）库；已有环境本次只新增该库，没有升级其他依赖。

```powershell
.venv/Scripts/python.exe -m pip install --only-binary=:all: -r projects/sim2gse/requirements.txt
build-and-verify build --project .
build-and-verify verify --project .
```

构建使用 `compatibility/lock.json`（兼容锁）中固定的上游提交、源归档、补丁顺序和构建参数。当前准备入口读取本机已有官方归档 `.local/gear-planner-dk-validation/b845947-source.zip`，核对散列后在 `.tools/sim2gse/product/` 独立准备原版和受控版；该归档只是只读来源，不调用或修改配装器引擎。无归档或编译工具时明确失败，尚未实现全新机器的自动下载准备。

每次构建记录真实程序散列；任务开始前核验构建状态、程序和补丁身份。引擎使用任务目录内固定的相对文件名，从而支持用户输入和任务目录中的中文及空格。程序关闭联网，角色不会自动上传。

## 验证边界

- 原生试验开关完全由输入配置决定：`allow_experimental_specializations=1`（开启）或 `=0`（关闭）按原生要求放在角色声明前；未提供则使用原生默认值。基准和受控批次使用同一原始文本，产品不注入开关、不自动重试、不自行分类试验支持；原生拒绝、崩溃及报告错误会终止任务并保留日志。此前自动试验模式的验收属于历史行为。
- 原生自动生成队友时，以姓名和职业唯一定位输入角色，只控制该角色；队友沿用原生行动列表。优化与复测使用原生团队伤害，报告同时保留个人伤害。单角色继续使用个人伤害。
- 导出按钮使用原生动作的法术数据编号，运行时渠道编号仍保留观察；没有原生行动列表来源的派生效果不另造按键。

- `native_reference`（原生参考）使用自由选招；优化结果在 `final.scenarios`（最终复测情景）比较锁定候选与有效初始序列。无可信改善时输出初始序列；`locked_candidate_key`（锁定编号）与 `selected_candidate_key`（选用编号）分别记录这两个决定。
- 报告逐项比较编译动作块与原生实际解析块，按战斗编号、原始输入编号、步进和动作签名双向核对派发与执行计数；施法完成可以晚于派发，战斗末未完成施法单独核对。原生派生事件另行记录，不当作额外按键；未就绪动作按输入推进。
- 日常入口自动选择动作、搜索和复测。前两票的单次回归显式使用 `mode="single"`（单次模式），其 `program`（动作块）和 `phase_ms`（起始相位毫秒）仅用于诊断；单次模式不代表完成优化。
- 当前真实角色候选通过本机 CBOR 往返和固定上游导入后的编译核对；客户端接口使用明确的边界替身。
- 编码采用游戏 12.1 实测向量确认的无封装 Deflate（压缩格式）及 CBOR（紧凑对象编码）字节字符串，表的字符串键同样按字节串处理。`{1,"test"}` 的客户端输出为 `a2J0KUktLgEA`，已逐字节匹配；完整候选的游戏导入与按键仍需验收。
- 能力清单来自本次基准执行过的玩家动作，依据引擎原生归属、后台和被动等标记筛选；不维护人工辅助技能排除表。基准未使用的其他能力不阻断任务。原生战前动作单独记录，由引擎战前流程处理；战斗中选入但无法表达或执行的动作仍明确失败。范围是本次基准策略，不是职业技能全集。
- 基准引擎仅增加观测报告补丁，导出原生已有的整批累计执行次数与动作属性；没有改变原生选招或伤害模型。样例轨迹只用于起点顺序参考，不冒充完整批次。
- 不安装游戏插件，不写游戏目录，也不发送游戏按键。

自动检查使用人工构造的角色样例，覆盖输入保真、注入拒绝、动作块一致性、单技能、不可用步骤、队列覆盖、双主动物品顺序、重置与重放，以及引导饰品和超长宏的拒绝。浏览器回归包含基准未使用的种族及辅助天赋不阻断生成。本机真实角色的单次链由 `tests/sim2gse/manual_native_task.py` 验证，默认完整优化由 `tests/sim2gse/manual_search_task.py` 验证；该检查依赖未提交的用户样本，不能视为远端已运行。

## 当前验收记录

第二票的本机真实入口证据位于 `.local/sim2gse/implementation/任务02验收-20260912-r2/`。`acceptance.json`（验收摘要）记录输入与候选散列、18 个动作块的一致性及原生事件统计；`result.json`（结果）保留完整轨迹。100 次请求产生 99 个有效统计样本，每次战斗 180 秒。原生通知与仅保留为元数据的字段继续出现在报告中。游戏导入及按键仍未验收。

第三票的默认预算真实搜索证据位于 `.local/sim2gse/implementation/任务03验收-20260912-r2/`。累计计算 573.485 秒，入口墙钟 574.015 秒，评估 187 个候选、完成 666 个批次；四条局部搜索链完成 11 轮，在搜索期限停止并如实标记末轮未完成。最终五种节奏各完成 20 对独立批次，每个对象各有 1,980 个有效样本。相对有效初始序列，名义、扰动、慢按、暂停、相位的模拟伤害分别提高约 60.6%、59.6%、25.8%、57.1%、60.2%，各自配对差值的 95% 区间下界均大于零。该比较不代表超过自由选招参考，也不是游戏实测收益；`candidate.txt` 保存选用序列，游戏验收仍未执行。

按用户 2026-09-12 的安排，全仓构建与验证失败留到 PR（拉取请求）合并前统一处理；本票使用针对性测试与真实入口证据，不能据此宣称全仓验证通过。


第四票真实三步界面验收证据位于 `.local/sim2gse/implementation/任务04验收 中文 空格-20260912-r2/`：37 项项目测试通过，真实计算 585.875 秒，含浏览器操作 588.078 秒；五情景独立复测完整，剪贴板与受测候选一致，修改输入会清空旧结果。首轮暴露的 Windows（视窗系统）读报告与原子替换冲突已在共享发布入口修复，并经真实文件占用回归与第二轮长运行验证。首轮取消记录不算通过，游戏验收仍待第五票。


2026-09-13 基准驱动修正：已移除能力全集前置检查及人工辅助分类表；40 项项目测试通过，真实用户原始字符串短预算浏览器复验通过，独立需求和规范审查无阻塞。验收摘要位于 `.local/sim2gse/implementation/基准驱动-20260913/acceptance.json`。此项修正未重新执行默认十分钟性能验收，前述第三、四票的性能数字属于旧动作来源版本，不能冒充本次新流程的性能结论。


## 2026-09-13：地面落点与输入间隔

用户确认地面技能统一在角色脚下释放，不再额外点选地面。导出按官方客户端 `SpellTargetRestrictions.flags` 的目的地位（64）生成 `[@player]` 宏；普通单位目标技能及已经由天赋提供目标位置的技能保持原类型。法术查询最终导出的编号，主动饰品查询原生使用效果的法术编号；单动作和同块动作使用同一路径，不维护职业技能白名单。

目标数据为本机正式服 **12.1.0.69814**，由固定 SimC（模拟引擎）提交自带的 CASC（游戏存储）提取器与 DB2（游戏数据表）解析器读取。`compatibility/spell-target-masks.json` 保存默认难度的原始非零掩码、原始文件散列和提取器提交，兼容锁核验快照散列。模拟数值数据仍固定为 **12.1.0.69587**；两个版本分别记录，不能宣称数值模型已升级。运行时不读取游戏目录。

页面可调整模拟按键间隔，默认 **300 毫秒**，允许 50 至 2000 毫秒整数。标准、波动、慢速、暂停和相位偏移情景均从该参数派生；配置进入任务和缓存身份。设置只影响模拟，不会替玩家按键，也不要求手动输入严格恒定。

角色脚下释放不代表远程目标必定被命中。当前模拟对照仍要求目标处于技能影响范围内；游戏中站位不满足时不能用模拟伤害作通过标准。完整地面落地与伤害表现等待用户复测。

数据复现：用固定上游 `casc_extract/casc_extract.py` 只读提取 FileDataID（文件编号）1139993，再用 `dbc_extract3/dbc_extract.py -t csv -b 12.1.0.69814` 和仓库固定的 `formats/12.1.0.69299.json` 解析 `SpellTargetRestrictions`，输出重定向到 CSV（逗号分隔文件）。随后执行 `scripts/dev/sim2gse/target-masks.py <csv> <db2> <output> --build 12.1.0.69814`。本次原始文件与中间结果位于 `.local/sim2gse/ground-target-data/`；工具依赖仅放在该目录下，未修改游戏或项目虚拟环境。
