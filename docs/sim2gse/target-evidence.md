# 邪恶角色样本与原生单目标取证

日期：2026-09-12。本轮按顺序处理[取得目标版本、角色与最小序列样本](../../myspec/changes/sim2gse-wayfinder/issues/05-target-evidence.md)，复核用户已提供的邪恶角色资料，并用本项目独立的官方原版参考程序完成一次单目标基线。此前越序取得的材料保留作历史对照，本轮新报告单独保存；本报告不代表 GSE（按键序列插件）执行模型或优化效率通过。

## 输入与版本

- 来源为用户本次会话粘贴的 02:40 导出，已与消息文本逐项比较一致；保存为 UTF-8（统一字符编码）、LF（换行符）及末尾换行，不声称保留了原始剪贴板字节。
- 原文保存在 `.local/sim2gse/target-evidence/unholy-20260912-0240.chat.txt`；运行副本为同目录 `unholy-20260912-0240.simc`。仅还原 80 处 `\_` 转义，未改变天赋、装备、附魔或启用背包物品。
- 原文 SHA-256（文件散列）：`3d0ff45e396f3f696c2c4e9b4c7eb4442b3998b2e88b27465751e9776fcf3f08`。
- 运行副本 SHA-256（文件散列）：`27181b0a92bb198a4266762d5f6fb4b6123c06d07786de58e5eae65a8cf5ee59`。
- 导出声明：SimulationCraft Addon（角色导出插件）`12.1.0-03`，游戏 `12.1.0.69587`，接口 `120100`。角色为 90 级至高岭牛头人、邪恶死亡骑士、输出职责。
- 原版参考程序为已核验的官方 `1210-01 / c1935b9`，完整提交 `c1935b92f40d1063696861b0f4f6701e714ebd09`，来源见[引擎研究](engine-research.md)。本轮程序位于 `.tools/sim2gse/reference/simc-1210.01.c1935b9-win64/simc.exe`，散列为 `f1281cdc7224d9d08e23cca1c090fa1b144b8b22491ffd8459ccefb8d8120987`。
- 为避免重复下载和重建未修改的成熟工具，本轮从此前已核验的官方程序缓存复制独立的普通文件；散列相同、文件身份不同且硬链接数为 1，配装器原文件未变。没有复制神圣或织雾改版，也没有把新运行指向配装器程序。来源与隔离记录在参考程序同目录的 `provenance.json`（来源清单）。这仅准备原版参考，按键控制扩展仍待后续独立源码与补丁构建。
- 新报告确认使用正式服 `69587` 数据，数据热修日期为 `2026-09-04`。构建号相同不代表已经逐项核验导出当天的在线热修。本轮重新读取了[官方发布任务](https://github.com/simulationcraft/simc-publish/actions/runs/34184702422)；包内程序身份沿用此前固定来源记录并重新校验散列，不把官方外层制品散列当成可执行文件散列。
- 本地原始文件与报告包含角色资料，保留在已忽略的 `.local/`，不纳入版本管理。

## 实际运行

复用已有解释器，调用本项目独立原版参考程序。本轮专用校验脚本为 `.local/sim2gse/target-evidence/task-05/check-sample.py`；它复用此前样本校验，仅调整为独立引擎、新报告目录和官方推荐的结构化输出参数。在仓库根目录可重跑：

```powershell
& '.venv/Scripts/python.exe' '.local/sim2gse/target-evidence/task-05/check-sample.py'
```

脚本只接受本样本所需的 26 个角色／装备字段，拒绝重复或额外活动字段，不接收动作覆盖、外部输入、输出路径或远程角色导入。它是本次取证脚本，不是完成了产品导入器及 T01/T02（输入测试）。物品来源固定为 `item_db_source=local`（本地物品数据）。

官方文档推荐 `json2`（结构化报告输出），并说明固定种子与线程设置的复现用途。本轮据此使用已有参数，不再逐一试验工具选项；实际报告仍核对版本与请求参数。[官方输出说明](https://github.com/simulationcraft/simc/wiki/Output)、[官方统计说明](https://github.com/simulationcraft/simc/wiki/StatisticalBehaviour)

| 参数或结果 | 本次证据 |
| --- | --- |
| 场景 | 静止单目标，固定 180 秒，无战斗长度扰动 |
| 计算 | 请求 100 次，1 线程，种子 `20260912`，不按目标误差提前停止 |
| 统计样本 | 报告计入 99 次；该版本多次模拟时不计第 0 次的数据收集，见本地 `engine/sim/sim.cpp:2010` |
| 增益与消耗品 | 关闭团队完整增益、药水、合剂、食物、强化符文与临时武器强化；属于本轮诊断条件，未升级为用户正式评分契约 |
| 动作 | 使用原生默认 APL（动作优先级列表），没有改成属性快照加等待 |
| 退出与报告 | 退出码 0，产生结构化报告、文本报告和进程日志 |
| 角色核对 | 专精、等级、种族、天赋字符串一致；15 个报告装备槽位的物品编号一致，战袍不在原生装备报告中 |
| 实际战斗 | 有死亡缠绕等技能执行、宠物统计与非零伤害，战斗长度为 180 秒 |
| 原生参考数值 | 平均约 79337.48 DPS（每秒伤害）；仅记录本轮诊断结果，模型限制未解除前不用于优化效率或实战承诺 |

本轮完整参数、散列及运行信息在 `.local/sim2gse/target-evidence/task-05/evidence.json`；同目录保存 `native-baseline.json`（结构化报告）、`native-baseline.txt`（文本报告）和 `native-baseline.log`（进程日志）。本轮只执行一次，耗时约 0.568 秒；与旧资料比较的角色、装备、数据版本及伤害统计一致。对照记录为同目录 `comparison-with-prior.json`。耗时只代表短诊断，不能外推搜索速度。

## 序列格式依据与真实样本

本票沿用已完成的[序列编码研究](gse-research.md)：研究快照为 `3.3.32`，固定提交 `f225d4c947d168c63451ef7c567d7063c38cc239`，已定位集合和单序列封装、导入与编译入口。这是后续离线原型的格式依据，不是用户已安装版本或用户最小导出样本。

实际插件版本、最小导出、绑定／重置配置及游戏行为仍按用户决议留到已有候选模拟结果和可导入产物后的真实导入阶段。本票没有生成假样本或提前制作编码原型；上述人工证据后置，不阻塞当前离线取证关闭。

## 尚未解除的限制

- 报告顶层日志提示主手符文不是 Apocalypse（天启）；这是原生模型提示，不是导出解析失败。本次保留用户附魔，不自动更换装备或天赋。
- 报告另有 `implementation_not_yet_verified`（实现尚未经验证）：`Rune of Unleashed Fire`（符文效果，中文名待核）的触发目标及伤害／治疗分支使用假设。必须进入动作与效果支持范围审查；没有通过报告存在或退出码 0 隐去此限制。
- 两件已装备饰品是虫群之瘤（编号 `250245`）与共鸣咆哮石（编号 `250228`），均解析为 311 装等。固定源码确认二者都是装备后的被动触发，不属于按键候选；保留装备输入，由原生引擎处理。本样本不能承担双主动饰品的验收，后续是否增加该范围另行决定，不为此更换用户装备。
- 虫群之瘤经驱动法术 `1250589` 触发 `crawling_plague`（伤害动作）与 `crawling_plague_heal`（治疗动作）；本次均平均执行约 13.9495 次，伤害非零，治疗量为零。固定本地 `engine/player/unique_gear_midnight.cpp:2940`、`:6157` 与 `engine/dbc/generated/item_effect.inc:10816` 可定位。
- 共鸣咆哮石经驱动法术 `1250564` 触发 `1254180` 与 `1254331` 两个增益；本次报告对应增益平均启动约 2.2626 和 2.6869 次。固定本地 `engine/player/unique_gear_midnight.cpp:2102`、`:6130` 与 `engine/dbc/generated/item_effect.inc:10807` 可定位。两件均有专用模型及实际触发证据，但这不等于触发率、缩放和正式服行为已逐项核实。
- 300 毫秒名义输入间隔继续有效，但本次原生 APL（动作优先级列表）不处理该按键节奏，不能把上述数值称为序列成绩。
- 用户明确要求先产出离线候选模拟结果，到真实导入验证阶段再讨论安装；同时必须严格按地图顺序。本轮只完成当前离线样本取证，离线研究假设不能冒充用户实际配置，也不授权提前执行支持范围或原型任务。
- 当前原生兼容性取证已具备版本、输入、独立程序和实际报告；完整支持清单、候选模型及游戏验收仍未通过。上述模型提示交给下一项审查，不因退出码为 0 自动解除。
