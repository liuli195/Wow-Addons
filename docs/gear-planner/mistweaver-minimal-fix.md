# 织雾最小修复与接入

日期：2026-09-11。用户授权先提交神圣圣骑士现有改动，再实施此前提出的最小修复。已先保存基线提交 `ec5f226`，随后才修改织雾。基线含前序职业共用适配及神圣接入；本轮未推送或合并。

## 实际改动

固定上游源码 `b845947a34429874433d8e9362326894650dd20a`，数据 `12.1.0.69587`。完整补丁在 `scripts/dev/gear-planner-research/mistweaver-attributes.patch`，相对于未修改源码只涉及武僧模块：

- 保留此前获准的两处实验性入口放行。
- 混合力量／敏捷／智力：织雾返回智力。
- 混合敏捷／智力：织雾返回智力，其余原有武僧专精继续返回敏捷。
- 其余混合类型、纯属性、通用数据、递减、护甲、取整、资源公式均未改。

碧火教诲没有增加补丁：原有通用被动解析已将 42965 耐力变为 `floor(42965 × 1.08) = 46402`。QE（治疗配装工具）遗漏它，不代表模拟器也遗漏；本次实测排除了重复添加倍率。

服务仅为织雾选择新程序，并设置治疗角色、目标等级 90，避免已复现的治疗目标 93 级初始化越界。神圣仍使用原先仅入口放行程序，其他专精继续使用此前程序。原程序摘要均未改变。

## 运行产物与重建

- 程序：`.tools/gear-planner-mistweaver/b845947-attributes1/simc.exe`。
- 摘要：`f1a8516e71a1d6e063fb83eca662cba5a5d56efe63639f711c68078fb8edbb06`。
- 隔离源码：`.local/gear-planner-mistweaver-research/source`。
- 原两处放行对照程序继续保留，没有覆盖。
- 编译使用现有 MinGW（编译工具），在 `engine` 目录执行 `mingw32-make -j2 SC_NO_NETWORKING=1 CXX=g++ MODULE=simc-mistweaver-attributes.exe`。复用同来源中间文件，重新编译武僧模块并链接，不声称全量干净重建。
- 从固定未修改源码重建时，只应用 `mistweaver-attributes.patch`；它已经包含入口放行，不要再叠加 `mistweaver-gate.patch`。
- 逐文件检查 cpp／hpp／h／inc（源码和数据），只有 `engine/class_modules/monk/sc_monk.cpp` 与固定原版不同。

产物、版本与验证范围记录在 `projects/gear-planner/fixtures/mistweaver-supported/manifest.json`。新程序约 141 MB，纳入任务 09.6 延后统一整理；本轮没有清理原程序或缓存。

## 计算状态

织雾页面新增简短计算状态说明：无临时增益，未计入罗盘周期姿态；不冒充实时快照。暗夜精灵显示实际采用的昼夜；赞达拉和土灵标明当前引擎默认洛阿／矿物。没有新增状态公式或擅自替换默认值。

这是当前基线模式的边界，不代表已经支持任意时刻的罗盘姿态、战斗分析层数或任意矿物输入。证据详见[规则清单](mistweaver-rule-map.md)。

## 验证

可重复执行：`python projects/gear-planner/check-mistweaver.py`。

| 场景 | 结果 |
| --- | --- |
| 无天赋构造样本 | 智力由 1531 改为 2942，敏捷由 1855 改为 618；改正混合贡献，不是游戏独立快照 |
| 卸胸甲／加回 | 智力 2613；加回 189 智力并应用皮甲倍率得到 2942，敏捷不变 |
| 卸披风 | 智力 2830，正确移除三合一智力贡献；耐力精确减少 2199 |
| 胸甲 334→337 | 智力 2942→2947，敏捷保持 618 |
| 碧火教诲 | 耐力 42965→46402，原有引擎路径已生效 |
| 治疗角色＋目标等级 90 | 完整属性快照与默认角色一致 |
| 四副属性评级 | 无额外修正基线与装备评级加总一致 |
| 酒仙、踏风 | 同一原版源码构建的未修改对照与新程序完整快照相同 |
| 宝石／胸甲三合一主属性附魔 | 属性发生预期变化，胸甲附魔增加智力、不增加敏捷 |
| 替换胸甲 | 合法候选可替换，保留当前装等和附魔；保存结构还原后重算一致 |
| 人类、血精灵、侏儒、暗夜精灵、至高岭牛头人 | 均可生成非空完整结果；此项不冒充各种族逐数值游戏验真 |
| 神圣圣骑士 | 原程序摘要未变；主属性、耐力、护甲与已保存参考一致 |
| 实际浏览器 | 独立浏览器环境导入织雾、显示计算状态、保存、刷新后主动加载成功，无页面脚本错误；没有操作用户浏览器存储 |

另运行输入校验、候选适配、异步响应、空状态及另存为现有检查。仓库快速检查通过；未执行远端检查或游戏内验收。

## 保留的限制

没有取得少量织雾游戏实测快照，因此本次完成的是最小修复、操作接入和本机回归，不宣称所有天赋／种族／取整边界都已验证。主属性修复没有修改任何基于猜测的统一倍率，也没有为通过测试放宽原有比较标准。

当前服务已重启加载新路由，原用户方案仍在浏览器中保存。回退织雾时可恢复基线提交中的服务及页面文件；各引擎原文件均仍保留。任务 9 的整体清理和此前明确暂不处理的问题仍按原记录执行。
