# 领域文档

采用单一上下文，遵循仓库文档集中在 `docs/` 的约定。

- 调查前读取 `docs/CONTEXT.md`（领域词汇表），以及 `docs/adr/` 中相关的 ADR（架构决策记录）。
- 涉及装备规划器时，同时读取现有 [装备规划器领域术语](../gear-planner/CONTEXT.md)。
- 文件不存在时继续工作；在实际确定术语或架构决策时按 domain-modeling（领域建模）技能创建。
- 需求、任务和设计使用词汇表中的术语。
- 发现与现有架构决策冲突时，明确指出冲突及依据。
